// ✅ JavaScript Variables and Calculations – Beginner Friendly

// 🧠 Example 1: Age calculation

let age = 23;          // We create a variable named 'age' and store 23 in it

age + 2;               // This adds 2 to age (23 + 2 = 25) but DOES NOT store or display the result

// ✅ To display the result, use console.log:
console.log("Age + 2 is:", age + 2);   // Output: Age + 2 is: 25

// ✅ Or store the result in a new variable
let newAge = age + 2;
console.log("New Age is:", newAge);   // Output: New Age is: 25



// 🧠 Example 2: Calculating Average Marks

let urdu = 80;         // Urdu marks
let eng = 90;          // English marks
let math = 100;        // Math marks

// Calculate the average
let avg = (urdu + eng + math) / 3;   // (80 + 90 + 100) / 3 = 270 / 3 = 90

// Display the average
console.log("Average Marks:", avg);   // Output: Average Marks: 90

// ✅ Summary:
// - 'let' is used to create variables
// - '+' is used for addition
// - '/' is used for division
// - 'console.log()' prints the result to the console
